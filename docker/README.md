# Docker

- `docker-compose.yml` defines db, backend, frontend services.
- `backend.Dockerfile` builds the Fastify API.
- `frontend.Dockerfile` builds the Next.js app.
- `backend.dockerignore` and `frontend.dockerignore` are per-service ignore files.
